main {
    int x = 5;
    int y;
    x++;
    if (x > 3)
    {
        #int x = 1;
        x -= 3;
    }
    if (x > 2 && x < 4)
        {x += 1;}
    else
    {
        bool y = false;
    }
    while (x < 10 || x < 9)
    {
        x++;
    }

    print(x);

    y = y == 0 ? 1 : -1;
    print(y);

    print(y / 2);
    y = 10;
    y /= 2;
    print(y);
}